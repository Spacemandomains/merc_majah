kind = "web"
previewPath = "/"
title = "Artist Directory Admin"
version = "1.0.0"
id = "artifacts/artist-admin"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23242

[services.development]
run = "pnpm --filter @workspace/artist-admin run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/artist-admin", "run", "build" ]
publicDir = "artifacts/artist-admin/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23242"
BASE_PATH = "/"
